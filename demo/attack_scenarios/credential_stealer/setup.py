from setuptools import setup, find_packages

setup(
    name="credential-stealer-demo",
    version="0.0.1",
    description="Harmless demo package with suspicious patterns",
    packages=find_packages(),
)
